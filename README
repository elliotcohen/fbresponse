Hi there!

RVM: Create a new gemset called 'whoshoulddateme' for Ruby 1.9.2

Then:

gem install bundler
bundle install
rake db:migrate
rails server
Navigate to http://localhost:3000 (it's important that you don't go to 0.0.0.0!)
Log in!
